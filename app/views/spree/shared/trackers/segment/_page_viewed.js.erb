<% if segment_enabled? %>
  <script>
    window.onload = function(){
      if (typeof analytics !== 'undefined') {
        if (!!getCookie('user_id') && !!getCookie('user_email')) {
          analytics.identify(getCookie('user_id'), {
          email: getCookie('user_email')
          })
        }
        else {
          analytics.identify({});
        }
      }
      analytics.page();
    };
  </script>
<% end %>
