<% if (@variant ||= Spree::Variant.find_by(id: params[:variant_id])) %>
  <% variant_ga_values = ga_line_item(@variant) %>
  <script>
    if (typeof gtag !== 'undefined') {
      gtag(
        'event',
        'add_to_cart',
        <%= variant_ga_values %>
      );
    }
  </script>
<% end %>
