<% if ga_enabled? %>
  <script>
    document.addEventListener('turbolinks:load', function() {
      if (typeof gtag !== 'undefined') {
        gtag('event', 'sign_up', {
        });
      };
    });
  </script>
<% end %>
