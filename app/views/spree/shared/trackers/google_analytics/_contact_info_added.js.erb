<% if ga_enabled? %>
  <script>
    document.addEventListener('turbo:load', function() {
      if (typeof gtag !== 'undefined') {
        gtag('event', 'contact_info_added', {
        });
      };
    });
  </script>
<% end %>
